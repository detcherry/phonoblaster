// ---------------------------------------------------------------------------
// TAPE MANAGER
// ---------------------------------------------------------------------------

TapeManager.prototype = new ScrollTabManager();
TapeManager.prototype.constructor = TapeManager;
